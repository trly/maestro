# GitHub Personal Access Token (legacy - prefer Settings UI)
# For new installs, configure tokens in the Settings page
# Tokens are stored using Tauri Store plugin in app data directory
# Environment variable still supported for backward compatibility
MAESTRO_GITHUB_TOKEN=
